# Secrets — never commit. All credentials live in GitHub Secrets / OIDC only (BUILD-PLAN §17).
.mcp.json
.env
.env.*
*.local.env
.factory/secrets/

# pipeline state lives on the orphan factory-artifacts branch (worktree), not main (§11)
.factory/

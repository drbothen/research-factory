---
description: "Stand up a new research market (instance) from the template — interview, config, seed, Actions, state, portfolio"
argument-hint: "<slug>"
---

Use the `research-factory init-market` skill via the Skill tool.

Arguments: $ARGUMENTS
